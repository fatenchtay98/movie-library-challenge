// Placeholder so `npm run prisma:seed` / `prisma db seed` is wired end-to-end.
// Real seed data (200+ movies, genres, admin + user accounts) lands with the
// Prisma schema in the next phase.

async function main(): Promise<void> {
  console.log('No seed data defined yet.');
}

main();
